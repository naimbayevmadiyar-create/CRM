-- УДАЛЕНИЕ ПРОБНЫХ ЗАЯВОК ПО НОМЕРАМ
--
-- Единственный оставшийся способ что-то удалить, и он намеренно ручной.
-- Очистки «за период» в системе нет: один неверно выбранный день — и вместе
-- с пробными заявками уходят настоящие, по которым клиентам выданы акты,
-- а вернуть их нечем.
--
-- Здесь удаляются только те заявки, номера которых вы перечислили сами.
-- Вместе с заявкой уходят её работы, история этапов и счета по ней.
-- Нумерация продолжается дальше: выданные клиентам документы не задвоятся.
--
-- Мастера, пароли, реквизиты компании, логотип и печать не трогаются.
--
-- ПЕРЕД ЗАПУСКОМ сделайте копию: Supabase → Database → Backups.
-- Удаление необратимо.

-- 1. Сначала посмотрите, что именно удалите, — замените номера на свои:

select number, created_at, client_name, client_phone, status, total_amount
from orders
where number in (1, 2, 3);

-- 2. Убедились, что это пробные, — выполните удаление с теми же номерами:

-- begin;
--
-- delete from invoices
-- where order_id in (select id from orders where number in (1, 2, 3));
--
-- delete from orders where number in (1, 2, 3);
--
-- commit;
